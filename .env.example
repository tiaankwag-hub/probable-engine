# Local development only — never a real secret. Copy to .env (git-ignored).
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/risk_platform
STORAGE_ROOT=./.data/storage

# Milestone 8 (AI provider). Get a key from https://aistudio.google.com/apikey.
# Leave blank to fall back to the deterministic mock provider.
GEMINI_API_KEY=
